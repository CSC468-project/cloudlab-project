pipeline {
    agent {
		kubernetes {
			defaultContainer 'buildct'
			yamlFile 'kubesbuilder.yaml'
		}
	}

    stages {
        stage('Build') {
			steps {
                echo 'Building mysql...'
				sh '(cd DockerFiles;docker-compose build mysql)'
			}
		}
        stage('Upload') {
			steps {
                echo 'Uploading to Docker registry...'
				sh '(cd DockerFiles;docker-compose push mysql)'
			}
		}
        stage('Deploy') {
			steps {
				echo 'Replacing running pods on k8s cluster'
                sh 'kubectl --namespace="default" replace --force -f mysql.yaml'
			}
	    }
    }	
}
