pipeline {
    agent {
		kubernetes {
			label 'buildct'
			defaultContainer 'buildct'
			yamlFile 'kubesbuilder.yaml'
		}
	}

    stages {
		stage('Setup') {
			steps {
				sh 'su; pacman -Sy'
				sh 'su; pacman -S docker docker-compose --noconfirm'
				sh 'su; dockered&'
			}
		}
        stage('Build') {
			steps {
                echo 'Building...'
				sh '(cd DockerFiles;su; docker-compose build)'
			}
		}
        stage('Upload') {
			steps {
                echo 'Uploading to Docker registry...'
				sh '(cd DockerFiles;su; docker-compose push)'
			}
		}
        stage('Deploy') {
            steps {
                sh 'kubectl replace deployment pizzapie -f pizzapie.yaml'
       	    }
	    }
    }	
}
