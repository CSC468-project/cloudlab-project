FROM archlinux


RUN pacman -Sy
RUN pacman -S python python-pip --noconfirm
RUN mkdir /dockerdata
WORKDIR /dockerdata


ENV FLASK_APP="app.py"
COPY flaskct_data/ /dockerdata/
RUN pip install -r /dockerdata/requirements.txt

CMD flask run --host=0.0.0.0
